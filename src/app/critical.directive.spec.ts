import { CriticalDirective } from './critical.directive';

describe('CriticalDirective', () => {
  it('should create an instance', () => {
    const directive = new CriticalDirective();
    expect(directive).toBeTruthy();
  });
});
